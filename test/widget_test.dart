import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Test de base — l'app nécessite un .env et des services
    // Les tests complets seront ajoutés avec les features
  });
}
